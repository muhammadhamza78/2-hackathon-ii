# Database connection
DATABASE_URL=sqlite:///./test.db   # Local dev DB (replace with Postgres URL in prod)

# JWT settings
JWT_SECRET_KEY=super-secret-dev-key
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Debug mode
DEBUG=True

# CORS: local frontend URLs
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
